# Databricks API Configuration
# Your Databricks workspace URL (e.g., https://adb-xxx.xx.azuredatabricks.net/)
DATABRICKS_HOST="https://adb-<your workspace uri>.azuredatabricks.net/"

# Your Databricks Personal Access Token (PAT)
# Generate this from the Databricks UI: User Settings > Access Tokens > Generate New Token
DATABRICKS_TOKEN="dapi_<your_token_here>"

# Server Configuration
# Host address for the server to listen on (default: 0.0.0.0) - Only declare if using SSE transport
SERVER_HOST="0.0.0.0"

# Port number for the server to listen on (default: 8000) - Only declare if using SSE transport
SERVER_PORT="8000"

# Enable debug mode for additional information (default: false)
DEBUG="false"

# Logging Configuration
# Available levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL="INFO"
